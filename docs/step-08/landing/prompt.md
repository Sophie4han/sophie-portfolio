# Landing extension — built-in ImageGen

Reference: existing WATER cutout. Source preserved as `extension-source.png` (1536×1024). Generated art is normalized with existing sharp: remove edge-connected white backdrop, scale uniformly, place on the original 1920×1080 raster. No app or manifest edits.

The first underlay composite left the original cliff in front of the connector. Placing the same generated extension above the original at the junction connects the walking surface. Only 2,131 originally opaque pixels at that junction differ (~5.4%); the lagoon and observation landmark are preserved. Added opaque area: 5,809 pixels (~14.6%).

```text
Use case: compositing. Reference is the existing INVADER water island; use its exact stone materials, isometric camera and upper-left soft daylight. Create ONLY a small attachable terrain extension sprite, no complete island, no water, no pavilion, no text or character.
The extension is a narrow continuous isometric stone causeway running diagonally from LOWER LEFT to UPPER RIGHT, with a compact rounded landing clearing at its LOWER LEFT end. At the UPPER RIGHT end the causeway broadens very slightly into a connector that will be tucked underneath the existing island's front-left dry shore. Camera/perspective matches reference. This is solid continuous terrain, not separate stepping stones, not a bridge floating above terrain.
The landing clearing at LOWER LEFT is flat pale warm-gray/cream dry stone with subtle natural surface texture, an open smooth interior with room for two feet, and visibly at least one character-width away from the cliff edges. No central cracks, boulders, bushes, ornaments, railings, water or dark markings on landing surface. The clearing should be small (roughly 1.6 times causeway width), rounded organically, not a huge circular disk. The path is about twice the pad diameter long, gently tapered, unobstructed and physically joined with no gaps. Underneath is a compact short weathered blue-gray rocky ledge with subtle moss only at exterior sides, much shorter cliff than the main island. No tall structures or vegetation.
Composition: single slim diagonal terrain strip with a rounder lower-left end, isolated on perfectly uniform pure white background, generous margins, entire object visible. Overall silhouette width:height about 1.5. All detail must match the attached island but existing island itself must NOT be drawn in this output. No cast shadow outside object. Prefer real transparent PNG, but if alpha is unavailable use uniform pure white, never checkerboard. High-resolution soft 2.5D miniature game illustration, no coarse pixels. This generated cutout will be composited underneath the existing island WITHOUT changing any existing island pixels.
```
